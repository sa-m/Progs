git init
git status
git add '*.*'
git commit -m "Message"
git log
git config --global user.email "sa99xm@gmail.com"
git config --global user.name "Sa_M"
git remote add origin https://github.com/sa-m/Progs
git add "*.*"
git push -u origin master 	//(-u = remembers the branch, from next onwards only git push)

